#include<stdio.h>
void main()


{
    int a,b;
    printf("Enter two no.");
    scanf("%d %d", &a,&b);

    if(a>b)
        { printf("a is larger than b"); }
    else
       {
         { printf("b is larger than a"); }


}
}
